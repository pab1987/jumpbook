import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const Color background = Color(0xFF0F1624); // fondo general
  static const Color card = Color(0xFF1A2333); // paneles, contenedores

  // Inputs
  static const Color inputBackground = Color(0xFF2A3450);
  static const Color placeholder = Color(0xFF6B7280);

  // Text
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFF9CA3AF);

  // Primary brand color
  static const Color primary = Color(0xFF2457FF);
  static const Color primaryHover = Color(0xFF3C6BFF);

  // Status colors
  static const Color error = Color(0xFFEF4444);
  static const Color success = Color(0xFF22C55E);
}
